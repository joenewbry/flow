# Audio Recording and Transcription Requirements
# Install with: pip install -r audio_requirements.txt

# Core audio processing
pyaudio>=0.2.11

# OpenAI API for Whisper transcription
openai>=1.0.0

# Environment variable management
python-dotenv>=1.0.0

# Audio analysis and processing
numpy>=1.21.0

# Database integration
chromadb>=0.4.0

# File handling and utilities
pathlib2>=2.3.7; python_version < '3.4'

# Note: Make sure you have the following system dependencies:
# macOS: brew install portaudio ffmpeg
# Ubuntu/Debian: sudo apt-get install portaudio19-dev python3-pyaudio ffmpeg
# Windows: Usually works out of the box with pip install pyaudio
# 
# IMPORTANT: You need an OpenAI API key in your .env file:
# OPENAI_API_KEY=sk-your-api-key-here

